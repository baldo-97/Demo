<nav class="navbar is-success" role="navigation" aria-label="main navigation">

<div id="navbarBasicExample" class="navbar-menu">
<div class="navbar-start">
  <%= link_to t('global.menu.Home'), root_path, class: 'navbar-item' %>
  <% if user_signed_in? %>
  <%= link_to t('global.menu.Products'), sales_path, class: 'navbar-item' %>
  <% end %>
</div>

<div class="navbar-end">
  <div class="navbar-item">
    <div class="buttons">
      <% if user_signed_in? %>
      <div class="navbar-item has-dropdown is-hoverable">
        <a class="navbar-link">
          <%= current_user.name %>
        </a>
        <div class="navbar-dropdown">
          <%= link_to t('global.menu.Log out'), logout_path, class: 'navbar-item' %>
        </div>
      </div>
      <% else %>
      <%= link_to t('global.menu.Sign up'), signup_path, class: 'button is-primary' %>
      <%= link_to t('global.menu.Log in'), login_path, class: 'button is-ligth' %>
      <% end %>
    </div>
  </div>
</div>
</div>
</nav>
